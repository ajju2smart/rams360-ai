# RAMS360 Frontend — Local Docker Environment
# Copy this file to .env if needed for local non-Docker development.
# For Docker: values are injected via docker-compose.yml build args.

NODE_ENV=development
PORT=3000

# Backend API — local Docker only
REACT_APP_API_URL=http://localhost:8000
